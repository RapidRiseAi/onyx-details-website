{
  "name": "onyx-details-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "predev": "node scripts/sync-uploaded-images.mjs",
    "prebuild": "node scripts/sync-uploaded-images.mjs",
    "precheck": "node scripts/sync-uploaded-images.mjs"
  },
  "dependencies": {
    "astro": "^6.0.6"
  }
}
